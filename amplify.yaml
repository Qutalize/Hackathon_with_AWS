version: 1
frontend:
  phases:
    build:
      commands:
        - sed -i "s|LAMBDA_FUNCTION_URL|${LAMBDA_FUNCTION_URL}|g" index.html
  artifacts:
    baseDirectory: /
    files:
      - "**/*"
  cache:
    paths: []

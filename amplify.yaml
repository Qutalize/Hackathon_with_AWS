version: 1
frontend:
  phases:
    build:
      commands:
        - echo "API_URL=${API_URL}"
        - sed -i "s|newproduct_proposal_api|${API_URL}|g" index.html
        - echo "=== 結果 ==="
        - grep "API_URL" index.html
  artifacts:
    baseDirectory: /
    files:
      - "**/*"
  cache:
    paths: []

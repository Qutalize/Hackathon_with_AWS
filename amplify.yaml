version: 1
frontend:
    phases:
        build:
            commands:
                - 'sed -i "s|newproduct_proposal_api|${API_URL}|g" index.html'
    artifacts:
        baseDirectory: /
        files:
            - '**/*'
    cache:
        paths: []
